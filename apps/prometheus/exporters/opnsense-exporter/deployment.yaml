apiVersion: apps/v1
kind: Deployment
metadata:
  name: opnsense-exporter
  namespace: prometheus-exporters
  labels:
    app: opnsense-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opnsense-exporter
  template:
    metadata:
      labels:
        app: opnsense-exporter
    spec:
      nodeSelector:
        affected_vlan: "50"
      containers:
        - name: opnsense-exporter
          image: ghcr.io/athennamind/opnsense-exporter:0.0.16@sha256:64469206eb955b6c87ec2737cd4884b0bec01b518408cf1ac6d5c153f6c0cf1f
          imagePullPolicy: IfNotPresent
          args:
            - "--opnsense.protocol=https"
            - "--opnsense.address=opnsense.home.hoareau-marion.eu"
            - "--exporter.instance-label=opnsense"
            - "--web.listen-address=:8080"
          ports:
            - containerPort: 8080
              name: fw-metrics
              protocol: TCP          
          envFrom:
            - secretRef:
                name: opnsense-secrets
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi