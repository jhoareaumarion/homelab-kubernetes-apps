apiVersion: v1
kind: Service
metadata:
  name: node-exporter-svc
  namespace: prometheus-exporters
spec:
  selector:
    app: node-exporter
  ports:
    - port: 9100
      targetPort: 9100
      name: http