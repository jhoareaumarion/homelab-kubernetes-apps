apiVersion: apps/v1
kind: Deployment
metadata:
  name: trek
  namespace: trek
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trek
  template:
    metadata:
      labels:
        app: trek
    spec:
      nodeSelector:
        affected_vlan: "50"
      securityContext:
        fsGroup: 1000
      containers:
      - name: trek
        image: mauriceboe/trek:4.2.1@sha256:777f4d647e973fe7d87fecd957e854b86d57e8d977fd041763e0ca19b3c2e2c0
        ports:
        - containerPort: 3000
          name: http
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
            add: ["CHOWN", "SETUID", "SETGID"]
        envFrom:
        - configMapRef:
            name: config-map
        - secretRef:
            name: secrets
        volumeMounts:
        - name: trek-data-vol
          mountPath: /app/data
        - name: trek-uploads-vol
          mountPath: /app/uploads
        - name: tmp-volume
          mountPath: /tmp
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 30
      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: trek-data-vol
        persistentVolumeClaim:
          claimName: data-pvc # Matches your created PVC
      - name: trek-uploads-vol
        persistentVolumeClaim:
          claimName: upload-pvc # Matches your created PVC
