apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mas-db-pvc
  namespace: synapse
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
