apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: moonlight
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: nfs-client