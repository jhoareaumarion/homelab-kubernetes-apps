apiVersion: v1
kind: Service
metadata:
  name: sparkyfitness-server
  namespace: sparky-fitness
spec:
  selector:
    app: sparkyfitness-server
  ports:
    - port: 3010
      targetPort: 3010
      protocol: TCP