apiVersion: gateway.nginx.org/v1alpha1
kind: ClientSettingsPolicy
metadata:
  name: actual-budget-client-policy
  namespace: actual-budget
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: actual-budget-route
  body:
    maxSize: 100Mi