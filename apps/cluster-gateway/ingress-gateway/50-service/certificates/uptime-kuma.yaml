apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uptime-kuma-hoareau-marion-eu-tls
  namespace: cluster-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  secretName: uptime-kuma-hoareau-marion-eu-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - uptime-kuma.hoareau-marion.eu