apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-private-hoareau-marion-eu-tls
  namespace: cluster-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  secretName: auth-private-hoareau-marion-eu-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - auth.private.hoareau-marion.eu