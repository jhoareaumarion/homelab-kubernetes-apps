apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: vlan50-pool-tcp-ingress
spec:
  blocks:
    - start: "10.10.50.151"
      stop: "10.10.50.151"
  serviceSelector:
    matchLabels:
      io.cilium/lb-pool: vlan50-pool-tcp-ingress