apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-consume-pvc
  namespace: sftpgo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
  volumeName: shared-sftpgo-pv
