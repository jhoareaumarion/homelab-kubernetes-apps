apiVersion: apps/v1
kind: Deployment
metadata:
  name: sftpgo
  namespace: sftpgo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sftpgo
  template:
    metadata:
      labels:
        app: sftpgo
    spec:
      nodeSelector:
        affected_vlan: "50"
      containers:        
        - name: sftpgo
          image: ghcr.io/drakkan/sftpgo:v2.7.4@sha256:6cb60f08fede46c5dc49a0c1d2994287ca440a63a3bd75b7f37ab2c0b24bbd06
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: cm
            - secretRef:
                name: sftpgo-secrets
          ports:
            - containerPort: 8080
            - containerPort: 2022
          volumeMounts:
            - name: sftpgo-home-volume
              mountPath: /var/lib/sftpgo
            - name: sftpgo-data-volume
              mountPath: /srv/sftpgo
            - name: shared-consume-volume
              mountPath: /mnt/paperless-consume
      volumes:
        - name: sftpgo-home-volume
          persistentVolumeClaim:
            claimName: sftpgo-home-pvc
        - name: sftpgo-data-volume
          persistentVolumeClaim:
            claimName: sftpgo-data-pvc        
        - name: shared-consume-volume         
          persistentVolumeClaim:
            claimName: shared-consume-pvc