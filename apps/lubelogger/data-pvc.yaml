apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: lubelogger
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client