apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: key-pvc
  namespace: lubelogger
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client