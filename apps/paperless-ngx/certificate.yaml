apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paperless-ngx-hoareau-marion-eu-tls
  namespace: cluster-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  secretName: paperless-ngx-hoareau-marion-eu-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - paperless-ngx.hoareau-marion.eu